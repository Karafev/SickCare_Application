<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@android:color/white">

    <!-- Titre de l'écran -->
    <TextView
        android:id="@+id/profile_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mon Profil"
        android:textSize="24sp"
        android:textColor="#000000"
        android:layout_gravity="center"
        android:paddingBottom="16dp"
        android:textStyle="bold"/>

    <!-- Explication sur le nom -->
    <TextView
        android:id="@+id/nom_explanation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nom: "
        android:textSize="14sp"
        android:textColor="#777777"
        android:padding="8dp"
        android:layout_marginBottom="8dp"/>

    <!-- Nom de l'utilisateur -->
    <TextView
        android:id="@+id/nom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nom: "
        android:textSize="18sp"
        android:textColor="#333333"
        android:padding="8dp"
        android:background="#f0f0f0"
        android:layout_marginBottom="8dp"/>

    <!-- Explication sur le prénom -->
    <TextView
        android:id="@+id/prenom_explanation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Prénom: "
        android:textSize="14sp"
        android:textColor="#777777"
        android:padding="8dp"
        android:layout_marginBottom="8dp"/>

    <!-- Prénom de l'utilisateur -->
    <TextView
        android:id="@+id/prenom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Prénom: "
        android:textSize="18sp"
        android:textColor="#333333"
        android:padding="8dp"
        android:background="#f0f0f0"
        android:layout_marginBottom="8dp"/>

    <!-- Explication sur les maladies -->
    <TextView
        android:id="@+id/maladies_explanation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Liste des maladies"
        android:textSize="14sp"
        android:textColor="#777777"
        android:padding="8dp"
        android:layout_marginBottom="8dp"/>

    <!-- Liste des maladies -->
    <TextView
        android:id="@+id/maladies"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Maladies: "
        android:textSize="18sp"
        android:textColor="#333333"
        android:padding="8dp"
        android:background="#f0f0f0"
        android:layout_marginBottom="8dp"/>

    <!-- Explication sur les aliments interdits -->
    <TextView
        android:id="@+id/aliments_explanation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Aliments interdits: "
        android:textSize="14sp"
        android:textColor="#777777"
        android:padding="8dp"
        android:layout_marginBottom="8dp"/>

    <!-- Liste des aliments interdits -->
    <TextView
        android:id="@+id/aliments"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Aliments interdits: "
        android:textSize="18sp"
        android:textColor="#333333"
        android:padding="8dp"
        android:background="#f0f0f0"
        android:layout_marginBottom="8dp"/>

    <!-- Bouton pour déconnexion ou autres actions (facultatif) -->
    <Button
        android:id="@+id/btn_retour"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Retour"
        android:layout_marginTop="16dp"
        android:layout_gravity="center_horizontal"
        />

</LinearLayout>
